<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partial-views/header') %>
</head>
<body>
    <a href="/">Back</a>
    <h1>My Skills</h1>
    <ul>
        <% skills.forEach(skill => { %>
            <li> <a href="skills/<%= skill.id%>"><%= skill.skill%></a> </li>
        <% }) %>
    </ul>
</body>
</html>